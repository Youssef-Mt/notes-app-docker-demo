# Copier ce fichier en .env et renseigner de vraies valeurs (jamais commitées).
# Aucune valeur réelle ne doit apparaître dans ce fichier .env.example.
DB_PASSWORD=changeme_app_password
DB_ROOT_PASSWORD=changeme_root_password
